# PRD: 인천공항 혼잡도 대시보드

- 문서 버전: v0.1 (초안)
- 작성일: 2026-07-23
- 작성자: (제품 담당자 기입)
- 상태: 검토 중 (13장 오픈 이슈 확인 필요)

---

## 1. 개요

### 1.1 배경
인천공항 이용객, 마중/배웅객, 공항 관련 종사자는 특정 시간대의 터미널별 입국장·출국장 혼잡도를 사전에 파악하고 싶어 한다. 공공데이터포털에서 인천공항 여객 혼잡도(예측) 데이터를 오픈 API로 제공하고 있으나, 원본 API는 raw JSON 형태로 제공되어 일반 사용자가 활용하기 어렵다.

### 1.2 목적
공공데이터포털의 인천공항 여객 혼잡도 API 데이터를 가져와 **날짜/시간을 선택하면 해당 시점 터미널(T1, T2)별 입국장·출국장 혼잡도를 숫자와 그래프로 시각화**해주는 웹 대시보드를 제공한다. 로그인한 사용자는 자주 확인하는 시간대를 즐겨찾기로 저장하고 관리할 수 있다.

### 1.3 제공 형태
- 웹 서비스 (반응형, 데스크톱/모바일 브라우저 대응)
- Vercel에 정적 SPA로 배포

---

## 2. 목표 및 성공 지표

| 목표 | 지표(예시) |
|---|---|
| 사용자가 원하는 시간대의 혼잡도를 빠르게 확인 | 날짜/시간 선택 후 결과 표시까지 소요 시간 |
| 터미널 간 비교를 통한 의사결정 지원 | 비교 그래프 화면 방문 비율 |
| 재방문 유도 | 즐겨찾기 등록 사용자 수, 재방문율 |
| 안정적인 데이터 제공 | 공공데이터 API 응답 실패율, 대체 안내 노출 비율 |

*구체적인 목표 수치는 서비스 오픈 후 재논의.*

---

## 3. 대상 사용자

- 인천공항 출국/입국 예정자 (혼잡 시간 회피 목적)
- 공항 마중·배웅객
- 공항 관련 업무 종사자(리무진버스, 주차대행, 렌터카 등)
- 비로그인 사용자: 조회만 가능
- 로그인 사용자: 조회 + 즐겨찾기 관리

---

## 4. 용어 정의

| 용어 | 정의 |
|---|---|
| 터미널(Terminal) | 인천공항 제1터미널(T1), 제2터미널(T2) |
| 입국장/출국장 | 터미널 내 입국 심사 구역 / 출국 심사 구역 |
| 혼잡도 | 공공데이터포털 API가 제공하는 혼잡 지표(예: 대기인원, 혼잡 단계 등 — 실제 필드는 8장 참고 API 확인 후 확정) |
| 혼잡도 예측 API | 공공데이터포털에서 제공하는 "인천국제공항공사_출입국 혼잡도 예측 정보" 계열 API (정확한 API명·엔드포인트는 개발 착수 전 최종 확인 필요) |

---

## 5. 시스템 아키텍처 개요

```
[사용자 브라우저]
   ├─ React + Vite SPA (Vercel 정적 호스팅)
   │     ├─ 공공데이터포털 혼잡도 API 직접 호출 (클라이언트 사이드 fetch)
   │     │     └─ API 서비스 키는 .env(로컬) / Vercel 환경변수(배포)에서 주입
   │     └─ Supabase JS Client
   │           ├─ Supabase Auth (이메일/비밀번호 로그인)
   │           └─ Supabase Database (즐겨찾기 테이블, RLS 적용)
   │
   └─ (백엔드 서버 없음 — 서버리스/BaaS 조합)
```

- **원칙**: 별도의 자체 백엔드 서버를 두지 않는다.
- 공공데이터 API 호출: 프론트엔드에서 직접 호출 (사용자 지시에 따름).
- 즐겨찾기 데이터: Supabase Database에 저장하고 RLS로 사용자별 접근을 제한한다.
- 인증: Supabase Auth.

> ⚠️ 이 구조는 API 키가 브라우저 번들/네트워크 요청에 그대로 노출되는 것을 전제로 한다. 관련 리스크와 대응 방안은 11장 참고.

---

## 6. 핵심 기능 명세

### 6.1 오늘/내일 데이터 불러오기
- 서비스 진입 시 기본값으로 **오늘 날짜, 현재 시간대(또는 가장 가까운 예보 슬롯)**의 혼잡도를 자동 조회하여 표시한다.
- 날짜 선택 UI에는 "오늘" / "내일" 두 개의 탭(또는 버튼)을 우선 제공한다. (공공데이터 API가 오늘/내일 예측 정보만 제공하는 것으로 파악 — 8장 확인 필요)
- 데이터 로딩 중 로딩 인디케이터, 실패 시 에러 메시지 및 재시도 버튼을 노출한다.

### 6.2 날짜·시간 선택 후 상세 보기
- 사용자가 날짜(오늘/내일)와 시간(예: 1시간 또는 30분 단위 슬롯)을 선택하면 해당 시점의 터미널별(T1, T2) × 구역별(입국장, 출국장) 혼잡도를 조회한다.
- 결과는 다음 두 가지 형태로 함께 표시한다.
  - **숫자**: 혼잡도 수치/등급을 카드 또는 표 형태로 표시
  - **그래프**: 터미널·구역별 막대 또는 게이지 그래프
- 선택한 시간대가 API가 제공하지 않는 범위(과거, 오늘/내일 이외)인 경우 선택 자체를 막거나 안내 메시지를 표시한다.

### 6.3 터미널별 비교 그래프
- 동일 시간대 기준으로 T1 vs T2 혼잡도를 나란히 비교하는 그래프를 제공한다. (Recharts 사용)
- 입국장/출국장을 그래프 상에서 구분(색상 또는 범례)하여 표시한다.
- 비교 그래프는 상세 보기(6.2) 화면 내 하나의 섹션으로 통합 제공한다(별도 화면 분리 여부는 화면 설계 단계에서 확정).

### 6.4 다크모드 전환
- 화면 우상단 등 고정 위치에 라이트/다크 모드 토글 제공.
- 사용자의 마지막 선택을 브라우저(localStorage)에 저장하여 재방문 시 유지.
- 최초 진입 시 OS/브라우저의 `prefers-color-scheme`을 기본값으로 반영.
- 로그인 사용자에 한해 다크모드 설정을 Supabase에도 저장할지 여부는 확인 필요(13장 오픈 이슈).

### 6.5 회원가입/로그인 및 즐겨찾기
- **인증**: Supabase Auth 기반 이메일/비밀번호 회원가입 및 로그인. (소셜 로그인은 범위 외)
- **즐겨찾기 저장**: 로그인 사용자는 현재 조회 중인 조건(날짜, 시간, 터미널, 구역 등)을 즐겨찾기로 저장할 수 있다.
- **즐겨찾기 조회**: 로그인 사용자는 본인이 저장한 즐겨찾기 목록만 조회할 수 있다.
- **즐겨찾기 삭제**: 로그인 사용자는 본인의 즐겨찾기 항목만 삭제할 수 있다.
- **데이터 접근 제어**: Supabase Database의 Row Level Security(RLS)를 이용해, 각 사용자는 `user_id`가 본인인 행에만 SELECT/DELETE(및 INSERT)가 가능하도록 정책을 설정한다. (자세한 스키마는 8장 참고)
- 비로그인 사용자가 즐겨찾기를 시도할 경우 로그인/회원가입 화면으로 유도한다.

### 6.6 혼잡도 임계치 강조 표시
- 상세 보기(6.2)의 터미널×구역별 카드에서, 혼잡도(인원수)가 **100명 이상**이면 해당 카드의 배경색을 빨간 계열로 강조 표시한다.
- 색상 강조는 기존 숫자 표시(6.2)를 대체하지 않고 함께 제공한다(12장 접근성 요구사항: 색상만으로 정보를 구분하지 않음).

---

## 7. 화면 정의서 (개요)

| 화면 | 주요 구성 요소 |
|---|---|
| 대시보드(메인) | 오늘/내일 탭, 날짜·시간 선택 UI, 터미널×구역 혼잡도 카드(숫자), 비교 그래프, 다크모드 토글, 로그인 상태 표시, 즐겨찾기 추가 버튼 |
| 로그인 | 이메일/비밀번호 입력, 회원가입 화면 이동 링크 |
| 회원가입 | 이메일/비밀번호(+확인) 입력, 약관 동의(범위 확인 필요) |
| 즐겨찾기 목록(마이페이지) | 본인 즐겨찾기 리스트, 각 항목 클릭 시 해당 조건으로 대시보드 이동, 삭제 버튼 |

> 상세 와이어프레임/컴포넌트 설계는 본 PRD 범위 밖이며, 별도 디자인 산출물에서 다룬다.

---

## 8. 외부 API 연동 명세 (공공데이터포털)

| 항목 | 내용 |
|---|---|
| 데이터 출처 | 공공데이터포털 "인천공항 여객 혼잡도(예측)" API |
| 인증 방식 | 서비스 키(Service Key) 쿼리 파라미터 방식 (공공데이터포털 발급) |
| 호출 위치 | 프론트엔드(브라우저)에서 직접 fetch/axios 호출 |
| 제공 범위 | 오늘/내일 예측 데이터로 파악 (정확한 제공 범위·갱신 주기는 **API 문서 재확인 필요**, 13장 참고) |
| 응답 형식 | XML/JSON (공공데이터포털 API 특성상 XML 기본 제공, JSON 파라미터 지원 여부 확인 필요) |

> ⚠️ 실제 API명, 엔드포인트, 요청/응답 필드, 호출 제한(트래픽 제한)은 개발 착수 전 공공데이터포털에서 최종 확인 후 본 문서에 반영한다.

---

## 9. 데이터 모델 (Supabase)

### 9.1 인증
- Supabase Auth 기본 `auth.users` 테이블 사용 (이메일/비밀번호).

### 9.2 즐겨찾기 테이블 (`favorites`)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid (PK, default gen_random_uuid()) | 즐겨찾기 고유 ID |
| user_id | uuid (FK → auth.users.id) | 소유 사용자 |
| terminal | text | T1 / T2 |
| zone | text | 입국장 / 출국장 |
| target_date | date | 대상 날짜 |
| target_time | time (또는 text 슬롯 코드) | 대상 시간(슬롯) |
| label | text (nullable) | 사용자가 지정한 즐겨찾기 이름(선택) |
| created_at | timestamptz (default now()) | 생성 시각 |

### 9.3 RLS(Row Level Security) 정책
- `favorites` 테이블에 RLS 활성화.
- 정책 예:
  - SELECT: `auth.uid() = user_id`
  - INSERT: `auth.uid() = user_id` (본인 명의로만 생성 가능)
  - DELETE: `auth.uid() = user_id`
  - UPDATE: 필요 시 동일 조건 (수정 기능 범위는 13장 확인 필요)
- 서비스 롤 키(service role key)는 프론트엔드에 절대 노출하지 않으며, 클라이언트는 익명(anon) 키 + RLS 조합으로만 접근한다.

---

## 10. 환경변수 및 키 관리

| 변수명(예시) | 용도 | 관리 위치 |
|---|---|---|
| `VITE_AIRPORT_API_KEY` | 공공데이터포털 서비스 키 | `.env.local`(로컬), Vercel Environment Variables(배포) |
| `VITE_SUPABASE_URL` | Supabase 프로젝트 URL | 동일 |
| `VITE_SUPABASE_ANON_KEY` | Supabase 익명(anon) 키 | 동일 |

- Vite 프로젝트 기준 클라이언트에 노출되는 환경변수는 `VITE_` 접두사를 사용한다(빌드 시 번들에 포함됨을 인지).
- `.env` 파일은 `.gitignore`에 포함하여 저장소에 커밋하지 않는다. `.env.example`을 제공해 필요한 변수 목록만 공유한다.
- Vercel 배포 시 Production/Preview/Development 환경별로 값을 등록한다.
- Supabase의 **service role key는 절대 프론트엔드/환경변수로 클라이언트에 내려주지 않는다** (RLS 우회 가능한 키이므로).

---

## 11. 보안 고려사항

- **공공데이터 API 키 노출**: 요구사항에 따라 API 키를 프론트엔드에서 직접 사용하며, 이는 브라우저 네트워크 탭/번들에서 키가 노출됨을 의미한다.
  - 리스크: 제3자가 키를 추출해 자신의 애플리케이션에서 재사용하거나, 트래픽 제한(쿼터)을 소진시킬 수 있음.
  - 완화 방안(선택적, 필요 시 적용): 공공데이터포털에서 도메인 제한/트래픽 제한 옵션 지원 여부 확인, 키 사용량 모니터링, 필요 시 이후 단계에서 서버리스 프록시로 전환 검토.
  - 본 프로젝트 범위에서는 위 리스크를 인지하고 감수하는 것으로 결정됨(사용자 확인 완료).
- **Supabase RLS**: 즐겨찾기 등 사용자 데이터는 RLS로 보호하며, anon key만 클라이언트에 배포한다.
- **인증 정보**: 비밀번호는 Supabase Auth가 자체적으로 해시 처리(bcrypt 등)하여 별도 구현 불필요.
- **HTTPS**: Vercel 배포 시 기본 제공되는 HTTPS를 사용한다.

---

## 12. 비기능 요구사항

- **반응형**: 데스크톱/태블릿/모바일 브라우저에서 레이아웃이 깨지지 않아야 한다.
- **브라우저 지원**: 최신 Chrome/Edge/Safari 기준 (IE 미지원).
- **성능**: 날짜/시간 변경 시 API 응답 대기를 제외한 UI 반응은 즉시(로딩 상태 표시)여야 한다.
- **접근성**: 색상만으로 혼잡도를 구분하지 않고 숫자/텍스트 라벨을 병기한다(색약 사용자 고려).
- **에러 처리**: 공공데이터 API 장애/응답 지연 시 사용자에게 명확한 안내(예: "혼잡도 정보를 불러올 수 없습니다") 및 재시도 옵션 제공.

---

## 13. 기술 스택 요약

| 영역 | 선택 |
|---|---|
| 프론트엔드 | React + Vite (SPA) |
| 배포 | Vercel |
| 차트 | Recharts |
| 인증/DB | Supabase (Auth: 이메일/비밀번호, Database + RLS) |
| 외부 데이터 | 공공데이터포털 인천공항 여객 혼잡도 API (프론트엔드 직접 호출) |

---

## 14. 마일스톤(제안)

1. 공공데이터포털 API 스펙 확정 및 테스트 호출
2. 대시보드 UI(오늘/내일, 날짜·시간 선택, 상세 보기) 구현
3. 터미널별 비교 그래프 구현
4. 다크모드 구현
5. Supabase Auth 연동(회원가입/로그인)
6. 즐겨찾기 CRUD + RLS 정책 적용
7. Vercel 배포 및 환경변수 설정
8. QA 및 오픈

*코드 작성은 본 PRD 확정 및 13장 오픈 이슈 정리 후 진행한다.*

---

## 15. Out of Scope (범위 외)

- 소셜 로그인(구글 등) — 이번 범위는 이메일/비밀번호만
- 자체 백엔드 서버 구축
- 공공데이터 API 키에 대한 서버 프록시(보안 강화) — 필요 시 차기 버전에서 검토
- 다국어(i18n) 지원
- 푸시/이메일 알림
- 관리자(Admin) 페이지

---

## 16. 오픈 이슈 / 확인 필요 사항

코드 작성 전 아래 항목에 대한 답변/결정이 필요합니다.

1. **공공데이터 API 정확한 명세**: 실제 API명, 엔드포인트 URL, 요청 파라미터, 응답 필드(혼잡도가 숫자인지/단계인지), 오늘·내일 외 데이터 제공 여부, 시간 슬롯 단위(30분/1시간), 갱신(예측 업데이트) 주기를 알고 계신가요, 아니면 함께 문서/포털에서 확인해야 할까요?
2. **혼잡도 지표의 정의**: "혼잡도"가 대기인원 수, 혼잡 단계(여유/보통/혼잡 등), 퍼센트 중 어떤 형태인가요? (API 응답 구조에 따라 달라짐)
3. **즐겨찾기 개수 제한**: 사용자당 즐겨찾기 저장 개수에 제한을 둘까요, 무제한일까요?
4. **즐겨찾기 수정 기능**: 저장된 즐겨찾기의 이름(label)을 수정하는 기능이 필요한가요, 삭제 후 재등록으로 충분한가요?
5. **회원가입 이메일 인증**: Supabase의 이메일 인증(컨펌 메일) 절차를 사용할까요, 즉시 가입 완료로 처리할까요? 비밀번호 재설정(찾기) 기능이 필요한가요?
6. **다크모드 설정 저장 범위**: 다크모드 선택을 브라우저(localStorage)에만 저장할까요, 로그인 사용자는 Supabase에도 저장해 기기 간 동기화되게 할까요?
7. **약관/개인정보처리방침**: 회원가입 시 이용약관 및 개인정보처리방침 동의 절차가 필요한가요? (실 서비스 오픈 시 법적 요건 검토 필요)
8. **초기 화면 진입 시 위치 정보 사용 여부**: 터미널 기본값을 사용자가 직접 선택하게 할지, 아니면 기본값(T1)을 지정할지.
9. **트래픽/쿼터 대응**: 공공데이터포털 API의 일일 호출 한도를 알고 계신가요? (프론트 직접 호출 특성상 사용자 수가 늘면 한도 초과 가능성 존재)

---

## 검증 요약

위 문서(PRD.md)를 작성했습니다. 목차 및 핵심 내용은 응답 메시지에서 별도로 요약해 드립니다.
